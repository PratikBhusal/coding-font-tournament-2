---
import BaseLayout from "../layouts/BaseLayout.astro";
import AppShell from "../layouts/AppShell.astro";
import Header from "../components/Header.astro";
import Controls from "../components/Controls.astro";
import TournamentSidebar from "../components/TournamentSidebar";
import TournamentBoard from "../components/TournamentBoard";
import codingFonts from "../lib/codingFonts";
import { highlightAllSamples, highlightAllSampleLines } from "../lib/highlight";

const highlighted = await highlightAllSamples();
const highlightedLines = await highlightAllSampleLines();
---

<BaseLayout
  title="Coding Font Tournament"
  description="Crown your favorite coding font through a double-elimination bracket of head-to-head matchups across popular monospace fonts."
>
  <AppShell sidebarTestId="tournament-sidebar">
    <Header slot="header" path="/" />
    <Controls slot="controls" />
    <TournamentSidebar slot="sidebar" fonts={codingFonts} client:idle />
    <canvas
      id="canvas"
      class="pointer-events-none fixed inset-0 z-50 h-full w-full"></canvas>
    <TournamentBoard
      highlighted={highlighted}
      highlightedLines={highlightedLines}
      client:load
    />
  </AppShell>
</BaseLayout>
